---

definitions:
  options:
    type: object
    properties:
      force-sequential:
        type: boolean
    additionalProperties: false

type: object
properties:
  options:
    $ref: '#/definitions/options'
additionalProperties: false
