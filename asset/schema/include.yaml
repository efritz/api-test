---

definitions:
  stringOrList:
    oneOf:
      - type: string
      - type: array
        items:
          type: string

type: object
properties:
  scenarios:
    type: array
  include:
    $ref: '#/definitions/stringOrList'
additionalProperties: false
